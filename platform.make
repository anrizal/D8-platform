; API

api = 2

; Core

core = 8.x


; Drupal project.
; This will download the Drupal core version that the installation profile will
; be built on. This should be updated as newer versions of Drupal get released.

projects[drupal] = 8.0-alpha5

; adding patches to drupal core
; projects[drupal][patch][issue-number] = https://path.to/patch_file.patch
